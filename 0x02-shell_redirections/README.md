# Hello, World Bash Script

This repository contains a simple Bash script that prints "Hello, World" to the standard output. It's a basic example of a shell script and can serve as a starting point for learning shell scripting or as a template for more complex scripts.

## Usage

To run the script, follow these steps:

1. Open your terminal.

2. Navigate to the directory where the script is located.

3. Make the script executable if it's not already:
